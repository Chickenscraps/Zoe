/**
 * @zoe/discord-bot — Discord integration, commands, posting
 * M8: Full implementation (announcements, trade cards, commands)
 */
import { createLogger } from "@zoe/shared";

const log = createLogger("discord-bot");

log.info("Discord bot module loaded (stub)");

export { };
